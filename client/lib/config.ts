export const animationConfig = {
  tokenSpeed: 30, // ms per character for title streaming
  minCharDuration: 15, // ms for LiveThinking random animation
  maxCharDuration: 40, // ms for LiveThinking random animation
};
